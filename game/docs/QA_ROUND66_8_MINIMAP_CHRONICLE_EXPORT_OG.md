# QA_ROUND66_8 — План деревни (мини-карта), экспорт летописи, og:image демо, иконостас моб. фолбэк

- **Дата:** 2026-09-22
- **Коммит:** `7a86f51` — «патч 66.8: ПЛАН ДЕРЕВНИ … ЭКСПОРТ ЛЕТОПИСИ … OG-ДЕМО … ИКОНОСТАС моб. фолбэк …»
- **Основа:** бэклог отчётов 66.6/66.7 (приказов владельца в этом раунде не поступало; токен-авторизация восстановлена владельцем — «обновил права кода»).
- **Результат:** все 4 пункта закрыты; 501 юнит-проверка зелёная; прод = HEAD (хэши 9/9 MATCH).

---

## Что сделано

| # | Пункт | Реализация | Проверка |
|---|-------|------------|----------|
| 1 | Мини-карта «План деревни» (бэклог 66.6) | НОВЫЙ `systems/MiniMap.js`: виджет (canvas 78×45 ×2, пергаментная рамка, золото) в правом верхнем углу под названием деревни — клик разворачивает панель; панель 208×120 (8px/тайл): клетки из `world.buildMap()` (`cellColor`: улица/дорожка/частокол/ворота/дерево/вода/стены/двери/колодец), здания из `BUILDINGS` (`buildingColor`: церковь-золото, староста-синий, двор-янтарь, кузня-красный, знахарка-травяной, дома-коричневые), белая метка доски поручений `QUEST_BOARD_TILE` (23,4), золотые ворота; красная метка игрока — `VillageScene.update()` → `MiniMap.update()`, `Phaser.Math.Clamp` в границы; легенда 9 строк в 2 колонки; клавиша **P** + кнопка «🗺 План» в статус-баре (десктоп, рядом с «Обзор»); shutdown-destroy | юнит ×25 (палитра/геометрия/легенда/интеграция); стенд + прод: виджет 3 объекта, панель 27 объектов, метка живая; кадры |
| 2 | Экспорт летописи (бэклог 66.6) | `RusTime.buildChronicleExport(registry)` — чистая функция: шапка «ЛЕТОПИСЬ — «Летописи Руси»», `chronicleDateLine` (индикт/круги солнца-луны), месяц+иначе, косые часы+народное время, справка о дне, стиль новолетия, «ДЕЯНИЯ:» ← `actionLog.getFormattedText()` (или «пока ничего не записано»), подпись-ссылка; `exportChronicleFile(scene)` — Blob+a.click, ASCII-имя `letopis-<лето>-<мм>-<дд>.txt`, revokeObjectURL; запись «Записал летопись в файл.» в actionLog; кнопка «⬇ Экспорт летописи» в панели летописи (экспорт слева 200×28, закрыть справа 150×28); RU/EN ветки | юнит ×14 (канон текста, eraYear/realYearAD в датировке, null-безопасность, EN-ветка в коде); прод-eval: клик → `last: Записал летопись в файл. | details: {"file":"letopis-6914-11-02.txt"}` |
| 3 | Отдельный og:image демо (бэклог 66.6) | НОВЫЙ `assets/images/og-demo.jpg` 1200×630 (генератор `game/tools/make_og_demo_r68.py`: кадр `04-village.webp` центр-кроп, градиент+нижняя плашка, «ЛЕТОПИСИ РУСИ» DejaVuSerif-Bold 92 с двойной тенью, подзаголовок «Браузерная RPG-детектив · Русь XV века · BRP d100», золотая плашка-кнопка «sosnowda.github.io/game/», JPEG q88 ≈ 240КБ); `game/index.html`: og:image / twitter:image / JSON-LD image → og-demo.jpg, og:image:alt «…деревня Холм Великий, браузерное демо»; лендинг `index.html` остался на og-image.jpg | юнит ×11 (JPEG-сигнатура, SOF-размер 1200×630, вес 30–600КБ, 3 ссылки, отсутствие старых og-ссылок в game/index.html); визуальная проверка баннера |
| 4 | Иконостас моб. фолбэк — визуально (бэклог 66.7) | Код не менялся (рисовка 66.7 корректна). Проверка: `textures.remove('int_bg_church')` → `scene.start('Interior', {interiorId:'church'})` — путь `!painted && !hasBg` → `drawTileIconostasis` | Стенд-кадр `/tmp/66-8-iconostasis-tile.png`: 4 яруса икон, Царские врата, свечи, киоты (правый — «слово Божие — в сердцах»), аналой; дефектов вёрстки нет |

## Инфраструктура

- **SW v67** (`chronicles-ruthenia-v67`): game/index.html изменён → сайт-кеш инвалидируется; шапка sw.js дополнена записью v67. **game-assets-v21 НЕ тронут** (og-demo.jpg — файл сайта, network-first).
- **i18n**: +6 EN-ключей («План», «🗺 План деревни», «Жилой дом», «Ты», «⬇ Экспорт летописи», «Записал летопись в файл.»); `village.help.body` EN+RU упоминает «P — план деревни». Существующие переводы легенды подхватились автоматически («Постоялый двор» → The Wayside Inn, «Кузница» → Smithy, «Дом знахарки» → Healer's house).
- **Документы**: worklog.md (+секция 66.8), CHANGES.md («Патч 66.8»), AGENTS.md (§3 тесты 70/71, §4 правило 13 — мини-карта/экспорт/og-demo, §5 инструменты).

## Аудит и регрессии

- `node --check`: MiniMap.js, RusTime.js, i18n.js, VillageScene.js, test_round71.mjs — 0 ошибок; `sw.js` — parse OK.
- Юнит: **test_round71.mjs 70/70** (новый набор) + регрессии **64:40, 65:64, 66:48, 67:42, 68:54, 69:105, 70:78 = 431**; **итого 501 проверка, 0 провалено**.
- test_round69 актуализирован (og:image → og-demo.jpg; SW v67), test_round70 (SW v67) — канон раунда 66.8.

## Стенд-верификация (:8090, agent-browser)

1. `?shot=village` — виджет-план в правом верхнем углу (3 объекта: рамка/карта/метка), метка игрока живая (update в update()).
2. Панель (клик по виджету / `showPanel()`): заголовок «🗺 План деревни», план с цветными зданиями и метками, легенда 2×, «Закрыть» — 27 объектов, закрытие чистое.
3. Летопись (клик по статус-бару): кнопка «⬇ Экспорт летописи» рядом с «Закрыть»; eval-клик по подложке → запись в actionLog с именем файла.
4. Иконостас-фолбэк: тайловый вид церкви снят (кадр), 4 яруса/врата/киоты без дефектов.
5. EN (`?shot=village&lang=en`, свежая сессия): «🗺 Plan», «🗺 Village Plan», Church / Elder / The Wayside Inn / Smithy / Healer's house / Dwelling house / Job Board / Gate / You.
6. Консоль: без новых ошибок; известный артефакт headless-деградации (холодный вход → reload в сессии) — среда, не игра.

## Прод-верификация (sosnowda.github.io)

- Деплой: `1ed5439..7a86f51` → GitHub Pages build **built** (13:20:56 UTC); первые минуты serving-слой отдавал 404-статус с верным телом (известный самовосстанавливающийся сбой) — через ~3,5 мин всё сошлось.
- **Хэш-сверка 9/9 MATCH**: MiniMap.js, RusTime.js, i18n.js, VillageScene.js, game/index.html, sw.js, og-demo.jpg, CHANGES.md, worklog.md — прод = HEAD `7a86f51`.
- Прод-кадр `?shot=village`: виджет в углу, панель разворачивается (27 объектов), метка игрока, легенда; игра на проде работает.

## Риски/бэклог

- Виджет-план на проде в кадре `?shot=village` частично перекрыт оверлеем персонажа, который ставит ScreenshotDirector (только в постановочном кадре; в обычной игре поверх виджета — только погода/статус-бар, проверено локально).
- Кнопка «🗺 План» и клавиша P — десктоп; на мобиле разворот — кликом по виджету (кнопка в статус-бар не помещалась — следовать канону «Обзор»).
- Экспорт на iOS Safari может открыть .txt в новой вкладке вместо скачивания (стандартное поведение Blob+a.download; контент корректен).
- Git LFS для mp4, EN-глубина словаря — перенесены в бэклог следующих раундов.
